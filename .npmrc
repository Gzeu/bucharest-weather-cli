# ========================================
# 📦 GitHub Packages NPM Registry Config
# ========================================

# Configure GitHub Packages registry for @gzeu scope
@gzeu:registry=https://npm.pkg.github.com

# Alternative: Configure for organization scope if needed
# @your-org:registry=https://npm.pkg.github.com

# Authentication for GitHub Packages
# Note: GITHUB_TOKEN will be provided via environment variable
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# ========================================
# 🔐 Global NPM Configuration
# ========================================

# Default registry for non-scoped packages
registry=https://registry.npmjs.org/

# Publishing configuration
access=public

# ========================================
# 🚀 Performance & Security Settings
# ========================================

# Enable package-lock for consistency
package-lock=true

# Save exact versions (recommended for production)
save-exact=true

# Security audit level
audit-level=moderate

# Progress indicator
progress=true

# ========================================
# 📋 Development Settings
# ========================================

# Install dev dependencies by default
save-dev=false

# Engine strict mode
engine-strict=true

# Fund disabled for cleaner output
fund=false

# ========================================
# 🏷️ Usage Instructions
# ========================================

# To publish to GitHub Packages:
# 1. Set GITHUB_TOKEN environment variable
# 2. Update package.json with proper scope: @gzeu/package-name
# 3. Run: npm publish
#
# To install from GitHub Packages:
# npm install @gzeu/package-name
#
# To configure locally:
# echo "GITHUB_TOKEN=your_token_here" >> .env
